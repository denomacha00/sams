# Super Admin panel — use same-origin /api proxy in production (leave unset).
# Do NOT point at api.smart-managment.com — host check will block /super/* routes.
# VITE_API_BASE_URL=

# Local dev: backend API port (must match packages/backend PORT)
VITE_API_PROXY_TARGET=http://localhost:3001

# Dev overlay — same keys as secrets/providers.env.example
# Copy to packages/backend/.env.secrets (gitignored) or use secrets/providers.env on VPS.
# Canonical template: ../../secrets/providers.env.example
